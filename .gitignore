node_modules
dist

# Environment — keep secrets out of git (.env.example IS tracked)
.env
.env.local
.env.*.local

# Supabase CLI local artifacts
supabase/.temp
supabase/.branches

# Editor/OS
.DS_Store
__MACOSX

# Stray internal docs not part of the app
/BloodChain_2.0_Proposal_F.pdf

# Playwright / E2E artifacts — screenshots & reports must never be committed
/playwright-report
/test-results
/e2e
/.playwright
/.playwright-mcp
playwright/.cache
*.spec.ts-snapshots
/docs